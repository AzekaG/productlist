import React from 'react';

const ProductFilter = () => {
    return (
        <div>
            <button>ToDo</button>
            <button>Add</button>
            <button>All</button>
        </div>
    );
}

export default ProductFilter;
